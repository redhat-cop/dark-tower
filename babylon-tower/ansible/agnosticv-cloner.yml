- hosts: localhost
  gather_facts: false
  tasks:
  - name: Testing Username
    debug:
      var: '{{ lookup("env", "username") }}'
  - name: Testing SSH keys
    debug:
      var: '{{ lookup("env", "git_private_key") }}'
